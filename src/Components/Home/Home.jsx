import React, { use } from 'react';
import { AuthContext } from '../../Context/AuthContext';

const Home = () => {
  // const {name} = use(AuthContext)

  return (
    <div>
      this is home
    </div>
  );
};

export default Home;